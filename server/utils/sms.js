// Placeholder for SMS (e.g., Twilio integration)
const sendSMS = async (to, message) => {
  console.log(`SMS to ${to}: ${message}`);
  // Integrate with Twilio or another SMS service here
};

module.exports = { sendSMS };
